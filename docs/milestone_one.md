create flat map
generate terrain chunks
show terrain
mouse pick terrain
show brush preview
raise/lower height brush
rebuild dirty chunks


# /terrain owns terrain data, chunk generation, mesh/collider generation, and terrain queries. #
# /mapeditor owns user interaction, tools, UI, brush input, and save/load flow. #
